# Capture / deliverables

Required under `public/outreach/`:

- `before-original-desktop.png` — live original site, not a recreation
- `after-desktop.png` — ~1440px wide, full page preferred
- `after-mobile.png` — ~390px wide
- `after-scroll.gif` — meaningful scroll past the hero
- `after-scroll.mp4` — same

`/outreach` must preview all five. Captures are taken after visual QA, not before.

Validation: files open, dimensions sensible, no blank sections, GIF animates, MP4 plays.
